<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="Batch001Mapper">
    
    <select id = "findAll" resultType="com.example.demo.batch.vo.Batch001Vo">
        SELECT /* com.example.demo.batch.vo.Batch001Mapper - findAll */
               ID
             , NAME
          FROM TB_USER
    </select>

    <insert id="insertAll" parameterType="com.example.demo.batch.vo.Batch001Vo">
        INSERT /* com.example.demo.batch.vo.Batch001Mapper - insertAll */
          INTO TB_USER 
             ( 
               ID
             , NAME 
             ) 
        VALUES 
             ( 
               #{id}
             , #{name} 
             )
    </insert>
    
</mapper>